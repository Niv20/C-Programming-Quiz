// quizzes/pointers.js
const quizData = [
  {
    code: `#include <stdio.h>\n\nint main() {\n    int arr[] = {10, 20, 30, 40, 50};\n    int *ptr = arr + 1;\n    *ptr = 25;\n    *(ptr + 2) = 45;\n    printf("%d, %d\\n", arr[1], arr[3]);\n    return 0;\n}`,
    question: "מה יהיה הפלט של התוכנית?",
    answers: ["20, 40", "25, 45", "25, 40", "20, 45"],
    correct: 1,
    hint: "המצביע `ptr` מצביע על האיבר השני במערך. שינוי הערך דרך המצביע משנה את המערך המקורי.",
    explanation:
      "ptr מאותחל להצביע על `arr[1]`. השורה `*ptr = 25;` משנה את `arr[1]` ל-25. השורה `*(ptr + 2)` ניגשת שני איברים קדימה מ-`ptr`, כלומר ל-`arr[3]`, ומשנה את ערכו ל-45.",
  },
  {
    code: `#include <stdio.h>\n\nint main() {\n    int arr[] = {10, 20, 30, 40, 50};\n    int *ptr = arr + 1;\n    *ptr = 25;\n    *(ptr + 2) = 45;\n    printf("%d, %d\\n", arr[1], arr[3]);\n    return 0;\n}`,
    question: "מה יהיה הפלט של התוכנית?",
    answers: ["20, 40", "25, 45", "25, 40", "20, 45"],
    correct: 1,
    hint: "המצביע `ptr` מצביע על האיבר השני במערך. שינוי הערך דרך המצביע משנה את המערך המקורי.",
    explanation:
      "ptr מאותחל להצביע על `arr[1]`. השורה `*ptr = 25;` משנה את `arr[1]` ל-25. השורה `*(ptr + 2)` ניגשת שני איברים קדימה מ-`ptr`, כלומר ל-`arr[3]`, ומשנה את ערכו ל-45.",
  },
  {
    code: `#include <stdio.h>\n\nint main() {\n    int arr[] = {10, 20, 30, 40, 50};\n    int *ptr = arr + 1;\n    *ptr = 25;\n    *(ptr + 2) = 45;\n    printf("%d, %d\\n", arr[1], arr[3]);\n    return 0;\n}`,
    question: "מה יהיה הפלט של התוכנית?",
    answers: ["20, 40", "25, 45", "25, 40", "20, 45"],
    correct: 1,
    hint: "המצביע `ptr` מצביע על האיבר השני במערך. שינוי הערך דרך המצביע משנה את המערך המקורי.",
    explanation:
      "ptr מאותחל להצביע על `arr[1]`. השורה `*ptr = 25;` משנה את `arr[1]` ל-25. השורה `*(ptr + 2)` ניגשת שני איברים קדימה מ-`ptr`, כלומר ל-`arr[3]`, ומשנה את ערכו ל-45.",
  },
  {
    code: `#include <stdio.h>\n\nint main() {\n    int arr[] = {10, 20, 30, 40, 50};\n    int *ptr = arr + 1;\n    *ptr = 25;\n    *(ptr + 2) = 45;\n    printf("%d, %d\\n", arr[1], arr[3]);\n    return 0;\n}`,
    question: "מה יהיה הפלט של התוכנית?",
    answers: ["20, 40", "25, 45", "25, 40", "20, 45"],
    correct: 1,
    hint: "המצביע `ptr` מצביע על האיבר השני במערך. שינוי הערך דרך המצביע משנה את המערך המקורי.",
    explanation:
      "ptr מאותחל להצביע על `arr[1]`. השורה `*ptr = 25;` משנה את `arr[1]` ל-25. השורה `*(ptr + 2)` ניגשת שני איברים קדימה מ-`ptr`, כלומר ל-`arr[3]`, ומשנה את ערכו ל-45.",
  },
  {
    code: `#include <stdio.h>\n\nint main() {\n    int arr[] = {10, 20, 30, 40, 50};\n    int *ptr = arr + 1;\n    *ptr = 25;\n    *(ptr + 2) = 45;\n    printf("%d, %d\\n", arr[1], arr[3]);\n    return 0;\n}`,
    question: "מה יהיה הפלט של התוכנית?",
    answers: ["20, 40", "25, 45", "25, 40", "20, 45"],
    correct: 1,
    hint: "המצביע `ptr` מצביע על האיבר השני במערך. שינוי הערך דרך המצביע משנה את המערך המקורי.",
    explanation:
      "ptr מאותחל להצביע על `arr[1]`. השורה `*ptr = 25;` משנה את `arr[1]` ל-25. השורה `*(ptr + 2)` ניגשת שני איברים קדימה מ-`ptr`, כלומר ל-`arr[3]`, ומשנה את ערכו ל-45.",
  },
];
